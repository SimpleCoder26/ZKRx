# Use the exact Midnight Proof Server image required by the Ledger V8 dependencies
FROM midnightntwrk/proof-server:8.1.0

# Expose the standard proof server port
EXPOSE 6300

# Run the proof server
CMD ["midnight-proof-server", "-v"]
